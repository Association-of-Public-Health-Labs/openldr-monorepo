import { TextField } from "@mui/material";

export function ProvinceFilter() {
  return (
    <TextField
      disabled
      fullWidth
      label="Província"
      placeholder="Todas as províncias"
      size="small"
    />
  );
}
