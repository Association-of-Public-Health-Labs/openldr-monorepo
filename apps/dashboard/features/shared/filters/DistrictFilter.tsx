import { TextField } from "@mui/material";

export function DistrictFilter() {
  return (
    <TextField
      disabled
      fullWidth
      label="Distrito"
      placeholder="Todos os distritos"
      size="small"
    />
  );
}
