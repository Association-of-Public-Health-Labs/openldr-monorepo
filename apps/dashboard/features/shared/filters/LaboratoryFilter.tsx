import { TextField } from "@mui/material";

export function LaboratoryFilter() {
  return (
    <TextField
      disabled
      fullWidth
      label="Laboratório"
      placeholder="Todos os laboratórios"
      size="small"
    />
  );
}
