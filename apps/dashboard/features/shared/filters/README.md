# Filtros

Esta pasta fica reservada para utilitários futuros de filtros.

Na dashboard unificada, não há filtros globais nesta fase. Filtros de período,
laboratório, província, distrito ou unidade sanitária devem ser implementados
dentro de cada card/relatório, seguindo o padrão `MainCard` usado pela dashboard
TB existente.
