import { Tests } from '@/components/Tests'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'OpenLDR Analytics API',
  description: 'Documentação da API da plataforma OpenLDR Analytics para gestão de dados laboratoriais.',
}

export const sections = [
  { title: 'Testes', id: 'tests' },
]

<HeroPattern />

# OpenLDR Analytics API

A API OpenLDR Analytics fornece endpoints RESTful para relatórios e análises de testes laboratoriais em programas de diagnóstico de HIV e TB. Construída sobre a plataforma OpenLDR (Open Laboratory Data Repository), permite o acesso programático a dados laboratoriais agregados para vigilância e relatórios de saúde pública. {{ className: 'lead' }}

<div className="not-prose mt-4 mb-8 rounded-lg bg-zinc-50 p-4 dark:bg-zinc-800/50">
  <span className="block text-sm font-medium text-zinc-900 dark:text-white">
    URL Base: <code className="text-emerald-500">https://dev.openldr.org.mz</code>
  </span>
  <span className="mt-1 block text-sm text-zinc-600 dark:text-zinc-400">
    Autenticação: Token JWT Bearer via <code>POST /auth/login</code>
  </span>
</div>

<div className="not-prose mt-6 mb-16 flex gap-3">
  <Button href="/pt/quickstart" arrow="right">
    <>Início Rápido</>
  </Button>
  <Button href="/pt/openldr-data" variant="outline">
    <>Dados OpenLDR</>
  </Button>
</div>

<Tests />
