export const metadata = {
  title: 'HIV EID - Laboratory',
  description: 'Laboratory-level endpoints for HIV Early Infant Diagnosis analytics.',
}

export const sections = [
  { title: 'Common parameters', id: 'common-parameters' },
  { title: 'Tested samples by month', id: 'tested-samples-by-month', tag: 'GET', label: '/hiv/eid/laboratories/tested_samples_by_month/' },
  { title: 'Registered samples by month', id: 'registered-samples-by-month', tag: 'GET', label: '/hiv/eid/laboratories/registered_samples_by_month/' },
  { title: 'Tested samples', id: 'tested-samples', tag: 'GET', label: '/hiv/eid/laboratories/tested_samples/' },
  { title: 'TAT', id: 'tat', tag: 'GET', label: '/hiv/eid/laboratories/tat/' },
  { title: 'TAT samples', id: 'tat-samples', tag: 'GET', label: '/hiv/eid/laboratories/tat_samples/' },
  { title: 'Rejected samples', id: 'rejected-samples', tag: 'GET', label: '/hiv/eid/laboratories/rejected_samples/' },
  { title: 'Rejected samples by month', id: 'rejected-samples-by-month', tag: 'GET', label: '/hiv/eid/laboratories/rejected_samples_by_month/' },
  { title: 'Samples by equipment', id: 'samples-by-equipment', tag: 'GET', label: '/hiv/eid/laboratories/samples_by_equipment/' },
  { title: 'Samples by equipment by month', id: 'samples-by-equipment-by-month', tag: 'GET', label: '/hiv/eid/laboratories/samples_by_equipment_by_month/' },
  { title: 'Sample routes', id: 'sample-routes', tag: 'GET', label: '/hiv/eid/laboratories/sample_routes/' },
  { title: 'Sample routes viewport', id: 'sample-routes-viewport', tag: 'GET', label: '/hiv/eid/laboratories/sample_routes_viewport/' },
]

# HIV EID - Laboratory

Laboratory-level endpoints for HIV Early Infant Diagnosis (EID/DPI) analytics. These endpoints provide testing data grouped by testing facility, including sample counts, positivity rates, turnaround times, equipment usage, and geographic sample routing. {{ className: 'lead' }}

## Common parameters

<Row>
  <Col>
    Most laboratory endpoints accept the following query parameters for filtering and aggregating data. These parameters allow you to narrow results by date range, geographic location, facility type, and level of detail.

    <Properties>
      <Property name="interval_dates" type="JSON array">
        Date range filter as a JSON array with two dates in `YYYY-MM-DD` format: `["2025-01-01","2025-12-31"]`. Defaults to the last 12 months if not provided.
      </Property>
      <Property name="province" type="string">
        Filter by province name. Supports multi-select by passing the parameter multiple times.
      </Property>
      <Property name="district" type="string">
        Filter by district name. Supports multi-select by passing the parameter multiple times.
      </Property>
      <Property name="health_facility" type="string">
        Filter by a specific health facility name.
      </Property>
      <Property name="facility_type" type="string">
        Grouping level for aggregated results. Accepted values: `"province"`, `"district"`, `"health_facility"`.
      </Property>
      <Property name="disaggregation" type="string">
        Enable or disable data disaggregation. Accepted values: `"True"` or `"False"`.
      </Property>
      <Property name="lab_type" type="string">
        Filter by laboratory type. Accepted values: `"conventional"`, `"poc"`, `"all"`.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request with parameters" tag="GET" label="/hiv/eid/laboratories/tested_samples/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/tested_samples/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]' \
      --data-urlencode "province=Maputo Cidade" \
      --data-urlencode "facility_type=district" \
      --data-urlencode "disaggregation=True" \
      --data-urlencode "lab_type=conventional"
    ```
    </CodeGroup>
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Tested samples by month {{ tag: 'GET', label: '/hiv/eid/laboratories/tested_samples_by_month/' }}

<Row>
  <Col>
    Returns the number of EID samples tested per month, broken down by result (positive/negative) and sex. Use this endpoint to track monthly testing volume and positivity trends at the laboratory level.

    <Properties>
      <Property name="year" type="integer">
        The calendar year of the result.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The full name of the month.
      </Property>
      <Property name="total" type="integer">
        Total number of samples tested in that month.
      </Property>
      <Property name="positive" type="integer">
        Number of samples with a positive (HIV-detected) result.
      </Property>
      <Property name="negative" type="integer">
        Number of samples with a negative (HIV-not-detected) result.
      </Property>
      <Property name="female" type="integer">
        Number of samples from female patients.
      </Property>
      <Property name="male" type="integer">
        Number of samples from male patients.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/tested_samples_by_month/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/tested_samples_by_month/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "total": 1842,
    "positive": 87,
    "negative": 1755,
    "female": 934,
    "male": 908
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "total": 1763,
    "positive": 72,
    "negative": 1691,
    "female": 891,
    "male": 872
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Registered samples by month {{ tag: 'GET', label: '/hiv/eid/laboratories/registered_samples_by_month/' }}

<Row>
  <Col>
    Returns the total number of EID samples registered (received) at laboratories per month. Use this endpoint to monitor sample intake volume over time.

    <Properties>
      <Property name="year" type="integer">
        The calendar year of registration.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The full name of the month.
      </Property>
      <Property name="total" type="integer">
        Total number of samples registered in that month.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/registered_samples_by_month/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/registered_samples_by_month/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "total": 1956
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "total": 1874
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Tested samples {{ tag: 'GET', label: '/hiv/eid/laboratories/tested_samples/' }}

<Row>
  <Col>
    Returns the total number of EID samples tested per testing facility, with positive and negative breakdowns. Use this endpoint to compare testing output and positivity rates across laboratories.

    <Properties>
      <Property name="testing_facility" type="string">
        The name of the testing laboratory.
      </Property>
      <Property name="total" type="integer">
        Total number of samples tested at the facility.
      </Property>
      <Property name="positive" type="integer">
        Number of samples with a positive (HIV-detected) result.
      </Property>
      <Property name="negative" type="integer">
        Number of samples with a negative (HIV-not-detected) result.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/tested_samples/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/tested_samples/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "testing_facility": "INS Marracuene",
    "total": 5214,
    "positive": 243,
    "negative": 4971
  },
  {
    "testing_facility": "Hospital Central de Maputo",
    "total": 3867,
    "positive": 178,
    "negative": 3689
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## TAT {{ tag: 'GET', label: '/hiv/eid/laboratories/tat/' }}

<Row>
  <Col>
    Returns turnaround time (TAT) averages per testing facility, broken down by each segment of the sample transport and processing chain. Values represent the average number of days for each segment.

    <Properties>
      <Property name="testing_facility" type="string">
        The name of the testing laboratory.
      </Property>
      <Property name="collection_receiveHub" type="float">
        Average days from sample collection to hub reception.
      </Property>
      <Property name="receiveHub_registrationHub" type="float">
        Average days from hub reception to hub registration.
      </Property>
      <Property name="registrationHub_receiveLab" type="float">
        Average days from hub registration to laboratory reception.
      </Property>
      <Property name="receiveLab_registrationLab" type="float">
        Average days from laboratory reception to laboratory registration.
      </Property>
      <Property name="registrationLab_analyseLab" type="float">
        Average days from laboratory registration to sample analysis.
      </Property>
      <Property name="analyseLab_validationLab" type="float">
        Average days from sample analysis to result validation.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/tat/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/tat/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "testing_facility": "INS Marracuene",
    "collection_receiveHub": 3.2,
    "receiveHub_registrationHub": 0.5,
    "registrationHub_receiveLab": 2.1,
    "receiveLab_registrationLab": 0.8,
    "registrationLab_analyseLab": 1.4,
    "analyseLab_validationLab": 0.3
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## TAT samples {{ tag: 'GET', label: '/hiv/eid/laboratories/tat_samples/' }}

<Row>
  <Col>
    Returns the number of samples grouped by turnaround time ranges for a given TAT segment category. Use this endpoint to understand the distribution of processing times across defined intervals.

    In addition to the common parameters, this endpoint accepts the following:

    <Properties>
      <Property name="category" type="integer">
        The TAT segment to analyze. Accepted values: `1` (collection to hub reception), `2` (hub reception to hub registration), `3` (hub registration to lab reception), `4` (lab reception to lab registration), `5` (lab registration to analysis), `6` (analysis to validation).
      </Property>
    </Properties>

    <span className="block mt-4 font-medium">Response fields:</span>

    <Properties>
      <Property name="category" type="string">
        The name of the TAT segment being measured.
      </Property>
      <Property name="less_7" type="integer">
        Number of samples processed in less than 7 days.
      </Property>
      <Property name="between_7_14" type="integer">
        Number of samples processed in 7 to 14 days.
      </Property>
      <Property name="between_15_21" type="integer">
        Number of samples processed in 15 to 21 days.
      </Property>
      <Property name="greater_21" type="integer">
        Number of samples processed in more than 21 days.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/tat_samples/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/tat_samples/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]' \
      --data-urlencode "category=1"
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "category": "Collection to Hub Reception",
    "less_7": 4521,
    "between_7_14": 1203,
    "between_15_21": 387,
    "greater_21": 142
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Rejected samples {{ tag: 'GET', label: '/hiv/eid/laboratories/rejected_samples/' }}

<Row>
  <Col>
    Returns the total number of rejected EID samples per testing facility. Use this endpoint to identify laboratories with high rejection rates.

    <Properties>
      <Property name="testing_facility" type="string">
        The name of the testing laboratory.
      </Property>
      <Property name="total" type="integer">
        Total number of rejected samples at the facility.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/rejected_samples/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/rejected_samples/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "testing_facility": "INS Marracuene",
    "total": 312
  },
  {
    "testing_facility": "Hospital Central de Maputo",
    "total": 187
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Rejected samples by month {{ tag: 'GET', label: '/hiv/eid/laboratories/rejected_samples_by_month/' }}

<Row>
  <Col>
    Returns the number of rejected EID samples per month. Use this endpoint to track rejection trends over time.

    <Properties>
      <Property name="year" type="integer">
        The calendar year of the rejection.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The full name of the month.
      </Property>
      <Property name="total" type="integer">
        Total number of rejected samples in that month.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/rejected_samples_by_month/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/rejected_samples_by_month/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "total": 45
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "total": 38
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Samples by equipment {{ tag: 'GET', label: '/hiv/eid/laboratories/samples_by_equipment/' }}

<Row>
  <Col>
    Returns the number of EID samples processed per testing facility, broken down by equipment platform. Use this endpoint to analyze equipment utilization across laboratories.

    <Properties>
      <Property name="testing_facility" type="string">
        The name of the testing laboratory.
      </Property>
      <Property name="CAPCTM" type="integer">
        Number of samples processed on the Roche COBAS CAP/CTM platform.
      </Property>
      <Property name="ALINITY" type="integer">
        Number of samples processed on the Abbott Alinity platform.
      </Property>
      <Property name="M2000" type="integer">
        Number of samples processed on the Abbott m2000 platform.
      </Property>
      <Property name="C6800" type="integer">
        Number of samples processed on the Roche COBAS 6800 platform.
      </Property>
      <Property name="PANTHER" type="integer">
        Number of samples processed on the Hologic Panther platform.
      </Property>
      <Property name="MPIMA" type="integer">
        Number of samples processed on the Abbott m-PIMA platform.
      </Property>
      <Property name="MANUAL" type="integer">
        Number of samples processed manually.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/samples_by_equipment/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/samples_by_equipment/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "testing_facility": "INS Marracuene",
    "CAPCTM": 1245,
    "ALINITY": 987,
    "M2000": 1532,
    "C6800": 0,
    "PANTHER": 0,
    "MPIMA": 450,
    "MANUAL": 0
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Samples by equipment by month {{ tag: 'GET', label: '/hiv/eid/laboratories/samples_by_equipment_by_month/' }}

<Row>
  <Col>
    Returns the number of EID samples processed per month, broken down by equipment platform. Use this endpoint to track equipment utilization trends over time.

    <Properties>
      <Property name="year" type="integer">
        The calendar year.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The full name of the month.
      </Property>
      <Property name="CAPCTM" type="integer">
        Number of samples processed on the Roche COBAS CAP/CTM platform.
      </Property>
      <Property name="ALINITY" type="integer">
        Number of samples processed on the Abbott Alinity platform.
      </Property>
      <Property name="M2000" type="integer">
        Number of samples processed on the Abbott m2000 platform.
      </Property>
      <Property name="C6800" type="integer">
        Number of samples processed on the Roche COBAS 6800 platform.
      </Property>
      <Property name="PANTHER" type="integer">
        Number of samples processed on the Hologic Panther platform.
      </Property>
      <Property name="MPIMA" type="integer">
        Number of samples processed on the Abbott m-PIMA platform.
      </Property>
      <Property name="MANUAL" type="integer">
        Number of samples processed manually.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/samples_by_equipment_by_month/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/samples_by_equipment_by_month/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "CAPCTM": 412,
    "ALINITY": 305,
    "M2000": 498,
    "C6800": 0,
    "PANTHER": 0,
    "MPIMA": 127,
    "MANUAL": 0
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Sample routes {{ tag: 'GET', label: '/hiv/eid/laboratories/sample_routes/' }}

<Row>
  <Col>
    Returns the geographic routing of EID samples from requesting facilities to testing laboratories, including coordinates for mapping. Use this endpoint to visualize sample transport networks.

    <Properties>
      <Property name="requesting_facility" type="string">
        The name of the health facility that requested the test.
      </Property>
      <Property name="testing_facility" type="string">
        The name of the laboratory that processed the sample.
      </Property>
      <Property name="requesting_latitude" type="float">
        Latitude coordinate of the requesting facility.
      </Property>
      <Property name="requesting_longitude" type="float">
        Longitude coordinate of the requesting facility.
      </Property>
      <Property name="testing_latitude" type="float">
        Latitude coordinate of the testing laboratory.
      </Property>
      <Property name="testing_longitude" type="float">
        Longitude coordinate of the testing laboratory.
      </Property>
      <Property name="total" type="integer">
        Total number of samples transported along this route.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/sample_routes/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/sample_routes/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "requesting_facility": "CS Machava II",
    "testing_facility": "INS Marracuene",
    "requesting_latitude": -25.9623,
    "requesting_longitude": 32.5732,
    "testing_latitude": -25.7312,
    "testing_longitude": 32.6408,
    "total": 245
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Sample routes viewport {{ tag: 'GET', label: '/hiv/eid/laboratories/sample_routes_viewport/' }}

<Row>
  <Col>
    Returns sample route data filtered to a geographic bounding box, useful for map-based interfaces that only need routes visible in the current viewport. Accepts the same parameters as the sample routes endpoint, plus a viewport filter.

    In addition to the common parameters, this endpoint accepts the following:

    <Properties>
      <Property name="viewport" type="string">
        A comma-separated bounding box in the format `"south,west,north,east"` using decimal degrees. Only routes with at least one endpoint inside the bounding box are returned.
      </Property>
    </Properties>

    <span className="block mt-4 font-medium">Response fields:</span>

    <Properties>
      <Property name="requesting_facility" type="string">
        The name of the health facility that requested the test.
      </Property>
      <Property name="testing_facility" type="string">
        The name of the laboratory that processed the sample.
      </Property>
      <Property name="requesting_latitude" type="float">
        Latitude coordinate of the requesting facility.
      </Property>
      <Property name="requesting_longitude" type="float">
        Longitude coordinate of the requesting facility.
      </Property>
      <Property name="testing_latitude" type="float">
        Latitude coordinate of the testing laboratory.
      </Property>
      <Property name="testing_longitude" type="float">
        Longitude coordinate of the testing laboratory.
      </Property>
      <Property name="total" type="integer">
        Total number of samples transported along this route.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/eid/laboratories/sample_routes_viewport/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/eid/laboratories/sample_routes_viewport/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]' \
      --data-urlencode "viewport=-26.87,32.01,-25.00,33.50"
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "requesting_facility": "CS Machava II",
    "testing_facility": "INS Marracuene",
    "requesting_latitude": -25.9623,
    "requesting_longitude": 32.5732,
    "testing_latitude": -25.7312,
    "testing_longitude": 32.6408,
    "total": 245
  }
]
```
  </Col>
</Row>
