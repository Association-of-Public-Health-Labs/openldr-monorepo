export const metadata = {
  title: 'HIV VL - Summary',
  description: 'Summary and dashboard endpoints for HIV Viral Load analytics.',
}

export const sections = [
  { title: 'Common parameters', id: 'common-parameters' },
  { title: 'Header indicators', id: 'header-indicators', tag: 'GET', label: '/hiv/vl/summary/header_indicators/' },
  { title: 'Number of samples', id: 'number-of-samples', tag: 'GET', label: '/hiv/vl/summary/number_of_samples/' },
  { title: 'Viral suppression', id: 'viral-suppression', tag: 'GET', label: '/hiv/vl/summary/viral_suppression/' },
  { title: 'TAT', id: 'tat', tag: 'GET', label: '/hiv/vl/summary/tat/' },
  { title: 'Suppression by province', id: 'suppression-by-province', tag: 'GET', label: '/hiv/vl/summary/suppression_by_province/' },
  { title: 'Samples history', id: 'samples-history', tag: 'GET', label: '/hiv/vl/summary/samples_history/' },
]

# HIV VL - Summary

System-wide summary and dashboard endpoints for HIV Viral Load (VL) analytics. These endpoints provide aggregated indicators, viral suppression trends, turnaround times, and provincial breakdowns across all facilities. {{ className: 'lead' }}

## Common parameters

<Row>
  <Col>
    Most summary endpoints accept a common set of query parameters for filtering data by date range, geographic location, and facility attributes.

    <Properties>
      <Property name="interval_dates" type="JSON array">
        Date range filter as a JSON array with two dates in `YYYY-MM-DD` format: `["2025-01-01","2025-12-31"]`. Defaults to the last 12 months if not provided.
      </Property>
      <Property name="province" type="string">
        Filter by province name. Supports multi-select by passing the parameter multiple times.
      </Property>
      <Property name="district" type="string">
        Filter by district name. Supports multi-select by passing the parameter multiple times.
      </Property>
      <Property name="health_facility" type="string">
        Filter by a specific health facility name.
      </Property>
      <Property name="facility_type" type="string">
        Grouping level for aggregated results. Accepted values: `"province"`, `"district"`, `"health_facility"`.
      </Property>
      <Property name="disaggregation" type="string">
        Enable or disable data disaggregation. Accepted values: `"True"` or `"False"`.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request with parameters" tag="GET" label="/hiv/vl/summary/header_indicators/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/header_indicators/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]' \
      --data-urlencode "province=Maputo Cidade" \
      --data-urlencode "district=KaMpfumo" \
      --data-urlencode "facility_type=district" \
      --data-urlencode "disaggregation=True"
    ```
    </CodeGroup>
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Header indicators {{ tag: 'GET', label: '/hiv/vl/summary/header_indicators/' }}

<Row>
  <Col>
    Returns a single object with the main VL indicators for the selected period, including total registered, tested, suppressed, not suppressed, and rejected sample counts.

    <Properties>
      <Property name="registered" type="integer">
        Total number of samples registered in the system.
      </Property>
      <Property name="tested" type="integer">
        Total number of samples that have been tested.
      </Property>
      <Property name="suppressed" type="integer">
        Total number of samples with a suppressed viral load result.
      </Property>
      <Property name="not_suppressed" type="integer">
        Total number of samples with a not suppressed viral load result.
      </Property>
      <Property name="rejected" type="integer">
        Total number of samples that were rejected.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/header_indicators/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/header_indicators/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
{
  "registered": 185420,
  "tested": 172350,
  "suppressed": 148920,
  "not_suppressed": 23430,
  "rejected": 7840
}
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Number of samples {{ tag: 'GET', label: '/hiv/vl/summary/number_of_samples/' }}

<Row>
  <Col>
    Returns an array of monthly sample counts, providing a time-series view of total samples processed.

    <Properties>
      <Property name="year" type="integer">
        The year of the record.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The name of the month.
      </Property>
      <Property name="total" type="integer">
        Total number of samples in the month.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/number_of_samples/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/number_of_samples/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "total": 15840
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "total": 14920
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Viral suppression {{ tag: 'GET', label: '/hiv/vl/summary/viral_suppression/' }}

<Row>
  <Col>
    Returns an array of monthly viral suppression data, showing the breakdown of suppressed and not suppressed results over time.

    <Properties>
      <Property name="year" type="integer">
        The year of the record.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The name of the month.
      </Property>
      <Property name="suppressed" type="integer">
        Number of suppressed results in the month.
      </Property>
      <Property name="not_suppressed" type="integer">
        Number of not suppressed results in the month.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/viral_suppression/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/viral_suppression/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "suppressed": 12450,
    "not_suppressed": 1980
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "suppressed": 11870,
    "not_suppressed": 1840
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## TAT {{ tag: 'GET', label: '/hiv/vl/summary/tat/' }}

<Row>
  <Col>
    Returns an array of monthly turnaround time (TAT) averages broken down by each stage of the sample processing pipeline, from collection through validation.

    <Properties>
      <Property name="year" type="integer">
        The year of the record.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The name of the month.
      </Property>
      <Property name="collection_reception" type="float">
        Average days from sample collection to laboratory reception.
      </Property>
      <Property name="reception_registration" type="float">
        Average days from laboratory reception to sample registration.
      </Property>
      <Property name="registration_analysis" type="float">
        Average days from sample registration to analysis.
      </Property>
      <Property name="analysis_validation" type="float">
        Average days from analysis to result validation.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/tat/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/tat/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "collection_reception": 4.2,
    "reception_registration": 1.3,
    "registration_analysis": 2.8,
    "analysis_validation": 0.7
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "collection_reception": 3.9,
    "reception_registration": 1.1,
    "registration_analysis": 2.5,
    "analysis_validation": 0.6
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Suppression by province {{ tag: 'GET', label: '/hiv/vl/summary/suppression_by_province/' }}

<Row>
  <Col>
    Returns an array of viral suppression data grouped by province, showing suppressed and not suppressed counts for each province.

    <Properties>
      <Property name="province" type="string">
        The name of the province.
      </Property>
      <Property name="suppressed" type="integer">
        Number of suppressed results in the province.
      </Property>
      <Property name="not_suppressed" type="integer">
        Number of not suppressed results in the province.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/suppression_by_province/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/suppression_by_province/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "province": "Maputo Cidade",
    "suppressed": 18450,
    "not_suppressed": 2870
  },
  {
    "province": "Gaza",
    "suppressed": 14320,
    "not_suppressed": 2150
  }
]
```
  </Col>
</Row>

<div className="not-prose my-16 border-t border-zinc-900/5 dark:border-white/5" />

## Samples history {{ tag: 'GET', label: '/hiv/vl/summary/samples_history/' }}

<Row>
  <Col>
    Returns an array of monthly sample totals providing a historical view of testing volume over time. Useful for long-term trend analysis and capacity planning.

    <Properties>
      <Property name="year" type="integer">
        The year of the record.
      </Property>
      <Property name="month" type="integer">
        The month number (1-12).
      </Property>
      <Property name="month_name" type="string">
        The name of the month.
      </Property>
      <Property name="total" type="integer">
        Total number of samples in the month.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/hiv/vl/summary/samples_history/">
    ```bash {{ title: 'cURL' }}
    curl -G https://api.openldr.org.mz/hiv/vl/summary/samples_history/ \
      -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..." \
      --data-urlencode 'interval_dates=["2025-01-01","2025-12-31"]'
    ```
    </CodeGroup>

```json {{ title: 'Response' }}
[
  {
    "year": 2025,
    "month": 1,
    "month_name": "January",
    "total": 16230
  },
  {
    "year": 2025,
    "month": 2,
    "month_name": "February",
    "total": 15180
  }
]
```
  </Col>
</Row>
